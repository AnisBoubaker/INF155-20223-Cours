/*
Écrire une fonction qui identifie la position (indice) 
d'une valeur 'val' dans un tableau tab d'entiers.

La fonction doit retourner une valeur vraie si la valeur `val` est
dans le tableau ou fausse sinon. De plus, la position doit être fournie
par référence (pointeur).

Si la valeur se trouve plus d'une fois dans le tableau, la fonction
doit retourner la position de la première occurence.

Si la valeur `val` ne se trouve pas dans le tableau, retourner 
-1 comme position (pour indiquer qu'elle n'y est pas)

*/


#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>


int main(void)
{

}